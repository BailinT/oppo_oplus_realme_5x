# 交接：五线全量构建 + 包名正确性验证（2026-09-20 凌晨，跨电脑任务）

> 给接手的 AI：本任务从另一台电脑发起，那台电脑已关机。你（新电脑）从这里接手，**不需要**本机 AGENTS.md——关键规则都写在本文件内。

## 一句话任务

`BailinT/oppo_oplus_realme_all` 五条线全量构建（35 个子版本）已触发，**完成后逐包验证「文件名里的版本号 == Image 里编译的真实内核版本」**，向用户汇报对照表。重点防「名叫 6.6.89 实际是 6.6.118」。

## 5 个 run（main 分支，默认参数）

| 线 | run id | 触发时刻 (UTC) |
|---|---|---|
| 5.10 | 35455403020 | 09-19 16:34 |
| 5.15 | 35455412779 | 09-19 16:34 |
| 6.1 | 35455426105 | 09-19 16:35 |
| 6.6 | 35455438683 | 09-19 16:35 |
| 6.12 | 35455447536 | 09-19 16:35 |

写文档时状态：5.15、6.12 已 success；5.10、6.1、6.6 还在跑。35 个 build job 并行（公共 runner 并发 ~20 会排队），全部完成预计 2~3 小时。

另有一个**旧 run 35454798696**（6.1 线，用户自己手动触发的，旧命名代码）——它的产物是 8 个同名包，**不要下载归档**，仅作历史。

## 验证步骤

1. 等 5 个 run 全部 completed（`gh run list -R BailinT/oppo_oplus_realme_all` 或逐个 `gh api repos/BailinT/oppo_oplus_realme_all/actions/runs/<id>`）。
2. 某个 run 失败时：`gh run view <id> -R ...` 看哪个 job 挂了；**release job 挂且报 `tag_name already exists` → `gh run rerun <id> --failed` 即可救**（build 产物不受影响，tag 已加 SUB_VERSION 后缀但仍可能极端同秒）。
3. 全绿后跑验证脚本（仓库里已带，需 `gh auth login` 过）：
   ```
   python local/verify_naming.py
   ```
   脚本会自动下载 35 个 AK3 artifact → 解出 Image → 用正则提取 `Linux version X.Y.Z-...`（X.Y.Z 来自源码 Makefile，是编译时的唯一事实）→ 与文件名里的 `<KERNEL_VERSION>_<SUB_VERSION>` 段比对，输出逐包对照表。任何 MISMATCH 都要报给用户。
4. 产物**只用于验证，验证完 zip 已删**（脚本自动清理）。要不要正式归档下载，**问用户要位置**，规则：非 hwid 保留原始文件名；天玑包（文件名带 `-mtk` 或 SUB_VERSION 带 `_mtk`）归档时版本号后加「天」。

## 背景：这两天改了什么（commit 都已推送）

- **线级化**（c03c380 + 权限修复）：Actions 只剩 5 个线级入口（line_5.10/5.15/6.1/6.6/6.12.yml），35 个 fastbuild 转 workflow_call 模块。`only_sub_level` 留空=建全线、填=单建。
- **permissions 坑**（踩坑 22）：仓库默认 GITHUB_TOKEN=read，line caller 顶层必须声明 contents+actions+packages write，否则被调模块 startup_failure。
- **tag 坑**（踩坑 28）：release tag 已加 SUB_VERSION 后缀。
- **包名坑（本次验证的起因）**：6.1 全线 8 个子版本共用同一 KERNEL_NAME（android14-11-o-gca13bffobf09）→ 8 个包同名。已修：全线 35 文件的 AK3_NAME 在 KERNEL_NAME 前插入 `${SUB_VERSION}` 段（如 `..._6.1_115_android14-11-o-gca13bffobf09-mtk.zip`）。
- **天玑标识**：13 个天玑文件的包名带 `-mtk` 尾（5.10×5 + 6.1×4；6.6/6.12 的 SUB_VERSION 本带 _mtk 故无尾），workflow 名统一带 (天玑特供)。
- hwid 大仓（oppo_oplus_realme_all-hwid）**同构改动已推送**，但五线验证只在非 hwid 仓跑。若用户要 hwid 也验证：触发命令 `gh workflow run line_5.10.yml -R BailinT/oppo_oplus_realme_all-hwid --ref main`（其余线同理）。

## 坑速查（本机实战实证）

- **gh api 拉 jq 表达式可能被 MSYS 路径转换坑**（Linux 下无此坑）：复杂 jq 用 python requests/subprocess 包 JSON 处理。
- **火绒（如装有）会静默删刷机 zip**：批量下载前确认白名单。
- artifact 下载用 `gh api repos/.../actions/artifacts/<id>/zip` 直拉二进制（响应体就是 zip），别用 `gh run download`（会自动解包）。
- 失败的 run 不算产物。下载归档位置**必须问用户**。
- workflow 文件名触发比中文名稳：`gh workflow run line_6.6.yml`（不是名字）。

## 完成后

1. 向用户报 35 行对照表（文件名 vs Image 真实版本，全 OK 或列出 MISMATCH）。
2. 问用户是否归档下载、下到哪。
3. 本文件任务结束后删除或改为「已完成」状态。

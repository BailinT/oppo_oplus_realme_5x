# 5.10/5.15 新链交接（2026-09-18 凌晨，接手必读）

> **给接手电脑的话**：这份文档在 GitHub 仓里（`docs/交接-5x内核链-20260918.md`），不依赖任何本地路径。
> clone `BailinT/oppo_oplus_realme_5x` 到你本地任意目录（如 `C:\GKI\oppo_oplus_realme_5x`）即可。
> 需要 gh 已登录 BailinT 账号（或 git 凭证可推 main）。诊断日志不用拷，`gh run view <runId> -R BailinT/oppo_oplus_realme_5x --log` 随时可拉。

## 日志/文档同步约定（用户新增，必做）

**核心：让任何人打开 GitHub 就能看到最新进展，不用联系干活的人。**

1. **一轮一推**：每轮 workflow 跑完（无论成败）+ 修好下一个问题后，一个 commit 里同时包含：
   - 修复本身的改动
   - `docs/logs/run-<runId>.log`：该轮的**报错段摘录**（尾部 50-100 行即可，别传全量日志，全量用 `gh run view <id> --log` 随时拉）
   - 本文档「当前状态」区更新
2. **触发新构建前**必须先 push（文档里写明"改了什么、为什么、预期"）
3. **当前状态区永远置顶**，格式固定：
   ```
   ## 当前状态（<日期 时间>）
   - 最新 run：<id> <结论>
   - 挂在哪：<一句话>
   - 已做修复：<一句话>
   - 下一步：<一句话>
   ```
4. 长时间操作（编译中）不用反复推；**状态变更才推**，一天可能推多次也可能几小时一次
5. 换人/换电脑接手时：先 pull，读「当前状态」，再动手

---

## 总目标（用户原话定调）

把欧加真 **5.10/5.15** 内核做出来，形态照 cctv18 三仓（fastbuild，选对 x 覆盖同芯片所有机型）。
**先做无校验版（本仓），跑通后再做 hwid 校验版（oppo_oplus_realme_5x-hwid）。**
用户只卖校验版 AK3 包（¥35/位，一位一机）。速度：首次 30-60 分钟可接受，之后 ccache 生效 6-10 分钟。
（原电脑文件在 E:\DeepSeek-Reasonix\GKI，与本任务无关，新电脑不需要。）

## 当前状态（2026-09-18 02:55）

- 最新 run：首批 11 个 x 首跑收官：**226 绿**（sm8475 v_15，第二个绿），其余 10 个失败（含 167/180 是 runner 被取消，非代码问题）；第 6 轮 236 ccache 已存成功（311 MiB，key ccache-ecsv4-5.10.236）
- 挂在哪：四类失败——① 天玑树 Kconfig 引用开源仓不存在的 vendor 目录（mt6895: kernel/oplus_perf_sched/Kconfig 等）② SUSFS 补丁 VMA_PAD_START 只有调用无定义（5.10/5.15 补丁同病，209/123 实测；v_15/b_16 新代际树自带宏所以 226/236 绿）③ sm8550 dt 仓老分支没有 storage 替换源，模板硬 cp 直接杀死 init 步骤（74/123）④ 模板 make && 链踩 bash set -e 豁免——defconfig 失败步骤假成功，直到 KPM 才暴露
- 已做修复：12 个 yml 全量三件套——defconfig 缺失 Kconfig 自动 stub 循环（发现 Can't open file 即建空文件重试，最多 10 轮）+ VMA_PAD_START 条件 sed（树内无 define 才改成 vma->vm_end）+ qcom vendor-fix 非致命化 + make 显式失败即退
- 下一步：10 个失败 x 已全部重触发（03:00，修复版 workflow，commit e64d049）。明早收结果：`gh run list -R BailinT/oppo_oplus_realme_5x --limit 15`；MTK 大概率还会在 kbuild 阶段（Makefile obj 引用缺失目录，如 storage 驱动目录）出新错——defconfig stub 只兜 Kconfig 层，kbuild 层要按 modules 仓替换源补真身，那是下一层

### 第二轮 run id（03:00 重触发，修复版）

| x | run id |
|---|---|
| 5.10.66 | 35258041552 |
| 5.10.110 | 35258045288 |
| 5.10.149 | 35258048539 |
| 5.10.168 | 35258052109 |
| 5.10.198 | 35258055763 |
| 5.10.209 | 35258058827 |
| 5.15.74 | 35258063256 |
| 5.15.123 | 35258067023 |
| 5.15.167 | 35258071344 |
| 5.15.180 | 35258075111 |

### 首跑失败分类速查（2026-09-18 凌晨）

| x | run | 失败类别 |
|---|---|---|
| 5.10.66/110/149/168 | 35254926746/30546/34598/38055 | ① Kconfig 缺失 vendor 目录（oplus_perf_sched 实测） |
| 5.10.198 | 35254942072 | ① 同上（mt6983） |
| 5.10.209 | 35254946875 | ② VMA_PAD_START（task_mmu.c:353） |
| 5.10.226 | 35254950450 | **SUCCESS** |
| 5.15.74 | 35254954065 | ③ dt 仓 t_13.1 分支无 storage 替换源 → cp 致命 |
| 5.15.123 | 35254958290 | ③ + ②（task_mmu.c:304） |
| 5.15.167/180 | 35254962337/67082 | runner 取消（构建内核 step cancelled），重触发即可 |

排障实录（重要经验）：
- 模板 `make defconfig && make Image` 在 defconfig 失败时**步骤假成功**（set -e 对 && 链前半段豁免），错误要到 KPM 步骤 cd 不到 out/ 才暴露——已改为显式失败即退
- OnePlusOSS 仓的"死链"实际形态：b_16 等新仓是真 git symlink，更多情况是**开源仓直接不含该 vendor 目录**（连死链都没有），Kconfig source 指向空——所以修复从"替换死链"升级为"缺什么 stub 什么"
- Windows 上 git clone 会把 symlink 落成文本文件，本地 `find -xtype l`/`ls-files -s` 均不可用于盘点；且大内核仓 clone 耗时长，本机 _probe 下 4 棵树当时未 checkout 完导致误判"零缺失"——runner 日志才是唯一可信源
- 心跳/关机：Windows 计划任务 GKI_Heartbeat（每 20 分钟到 03:59）/GKI_AutoShutdown0400（04:00 关机，错过不补跑），日志 C:\GKI\heartbeat-20260918.log### 已建

- GitHub 仓：`BailinT/oppo_oplus_realme_5x`（public，main 分支）
- Fork：`BailinT/android_kernel_common_oneplus_sm8475`（fork 自 OnePlusOSS，备用）
- 样板 workflow：`fastbuild_5.10.236.yml`（基于 cctv18/oppo_oplus_realme_sm8650 的 fastbuild_6.1.118.yml 改造）
- 用户已授权全权操作（含 push 到本仓、触发构建）

### fastbuild_5.10.236.yml 相对 cctv 模板的全部差异（重要）

1. env：ANDROID_VERSION=android12 / KERNEL_VERSION=5.10 / SUB_VERSION=236 / KERNEL_NAME=android12-9-o-g5.10.236（**占位值，待真机 uname -r 校准**）/ CCACHE_KEY=ccache-ecsv4-5.10.236
2. 源码：OnePlusOSS/android_kernel_common_oneplus_sm8475 @ `oneplus/sm8475_b_16.0.0_ace_2`（=5.10.236）
3. **新增设备树仓下载**：android_kernel_modules_and_devicetree_oneplus_sm8475 @ 同分支
4. **新增 vendor-fix**（源码初始化步骤内）：
   - `drivers/soc/oplus/storage`（死链）← 拷 `devicetree/vendor/oplus/kernel/storage`
   - `kernel/oplus_cpu`（死链，init/Kconfig:857 source 它的 sched/Kconfig）← 拷 `devicetree/vendor/oplus/kernel/cpu`
   - `android/abi_gki_aarch64_oplus_internal` ← 从 `oppo-source/android_kernel_common_oppo_sm8475@oppo/sm8475_v_15.0.0_find_n2` 的同名文件下载
   - 删 `build.config.msm.lahaina/waipio/waipio.tuivm` 死链软链
5. SUSFS 源换 **ShirkNeko/susfs4ksu -b gki-android12-5.10**（cctv18/susfs4oki 无 5.x 分支），补丁名 50_add_susfs_in_gki-android12-5.10.patch；**去掉了 69_hide_stuff.patch**（6.1 专用）
6. **去掉了 Droidspaces 步骤和输入**（ntsync 是 6.6+ 特性）和 **CVE 补丁**（6.1 专用）
7. 默认值改动：kpm=true（用户惯例）、lz4/lz4kd=false、baseband_guard=false（5.10 未验证）
8. AnyKernel3 = **BailinT/AnyKernel3**（硬规则）；KSU FULL_NAME 后缀 @BailinT

### 编译进度（4 轮）

- 第 1 轮 35241761758：挂于缺 `aarch64-linux-gnu-as`（5.10 KMI gen9 用 -fno-integrated-as，Kconfig 探测要外部 GNU as）→ 已修：apt 装 `binutils-aarch64-linux-gnu`
- 第 2 轮 35242598691：挂于 `init/Kconfig:857` 找不到 `kernel/oplus_cpu/sched/Kconfig`（死链）→ 已修：vendor-fix 加 oplus_cpu
- 第 3 轮 35243151761：**编译深入 25 分钟（fs/lib/drivers 大面积编过，clang20 编 5.10 基本没问题）**，挂于 fs/statfs.c:88 隐式声明 `susfs_sus_kstat_spoof_vfs_statfs`——ShirkNeko 补丁自身缺陷：调用在 hunk 前部，extern 声明在同文件后面 hunk，susfs.h/susfs_def.h 均无该声明 → 已修：sed 往 fs/statfs.c 的 susfs_def.h include 之后注入 guarded extern 声明
- 第 4 轮 35247357129：第 3 轮修复生效（过了 statfs.c），13 分钟挂在 fs/susfs.c:284 隐式声明 `security_sb_statfs`——susfs.c 补了 statfs 逻辑副本但没 include <linux/security.h> → 已修：sed 给 fs/susfs.c 补该 include；日志摘录 docs/logs/run-35247357129.log
- **第 5 轮 35250898100：跑绿**（构建 18 分钟 + release）。KPM 补 Image、AK3 打包、ReSukiSU 35154 全链成功；AK3 已下载抽验（19 条目、Image 30.4MB、anykernel.sh 正常）。摘录 docs/logs/run-35250898100.log。注意：ccache 因空缓存命中未保存（见状态区），第 6 轮 ccache_update=true 补存

## 下一步（按顺序）

### ① 修 SUSFS 5.10 补丁声明缺失（✅ 两处均已验证：第 5 轮跑绿）

已修两处（模式：clang 报 implicit declaration → 找声明所在头文件 → workflow 补丁步骤末尾追加 sed 补 include / guarded extern）：

1. fs/statfs.c：susfs_def.h include 后注入 `#ifdef CONFIG_KSU_SUSFS_SUS_KSTAT` 包住的 extern 声明（susfs.h 无此声明，patch 本体又把声明放在调用之后）
2. fs/susfs.c：补 `#include <linux/security.h>`（284 行调 security_sb_statfs；两态可用）

主动扫描（第 4 轮后）：susfs.c 全部调用名逐一核对无第二处缺失；补丁往 security/selinux/{avc,hooks,selinuxfs}.c 插的 security_context_to_sid 等调用均有 selinux 子树自带头文件兜底。

### ② push 后重跑，预期还可能出现的坑（按概率排）

1. SUSFS 补丁还有其他前后失配的隐式声明（同模式修：把 extern 声明补进 susfs.h）
2. `CONFIG_KSU_SUSFS_SUS_MAP` 等配置项在 5.10 susfs 版不存在 → olddefconfig 自动丢弃，无害
3. clang20 编 5.10 个别文件报错 → 看具体文件，加针对性 KCFLAGS 或小补丁
4. 编过之后：KPM kptools 补 Image（通用）→ AK3 打包（BailinT/AnyKernel3，anykernel.sh 自带 kpn/zram 安装段）
5. 产物版本串 = `5.10.236-android12-9-o-g5.10.236`（KERNEL_NAME 是占位，**不影响 vendor 模块加载**——CONFIG_MODVERSIONS 开启时 same_magic 跳过版本号只比对 flag，已从 AGENTS.md 坑 4 的教训反推确认；但显示串待真机校准，问用户要一加11R/Ace2 的 uname -r）

### ③ 跑通后

1. 触发时勾 `ccache_update=true` 保存缓存（key ccache-ecsv4-5.10.236-none-Linux-main）→ 后续 6-10 分钟
2. 批量生成其余 11 个 x 的 workflow（复制 236 模板改 env/源码分支，全部平台×版本映射已写进 5x 仓 README.md，官方分支名我已全部实测核对）
3. 每个新 x 第一次跑都用「同芯片最新分支」，蓝灯后用户真机验证
4. 全部跑通 → 建 `oppo_oplus_realme_5x-hwid`（照现有 sm8650-hwid 模式：hwid/ 目录 + inject_hwid_lock.sh + workflow 注入步骤，allowlist 格式 `^[0-9a-z._:-]{4,128}$`）

## 参考坐标

- cctv 模板：`E:\DeepSeek-Reasonix\GKI\_cctv_sm8650\`（含 local/builder 脚本、lib、补丁）
- 5 系官方源码全部平台/分支/版本映射：已实测写入 `oppo_oplus_realme_5x/README.md`（5.10 共 8 个 x：66/110/149/168/198/209/226/236；5.15 共 4 个 x：74/123/167/180）
- SUSFS：ShirkNeko/susfs4ksu 分支 gki-android12-5.10 / gki-android13-5.15 / gki-android14-5.15（无 5.14+ 的用 5.15 对应 KMI）
- lz4kd：ShirkNeko/SukiSU_patch `other/zram/zram_patch/5.10|5.15/lz4kd.patch`（存在，已验证）
- cctv 公共缓存 cctv18/public_ccache：**无任何 5.x 预置**（44 个全是 6.x）→ 每个 x 首跑全量
- 每轮摘录：仓内 `docs/logs/`；全量诊断日志：`gh run view <runId> -R BailinT/oppo_oplus_realme_5x --log`（runId：35241761758 / 35242598691 / 35243151761 / 35247357129）；SUSFS 补丁原文：ShirkNeko/susfs4ksu gki-android12-5.10 分支 kernel_patches/50_add_susfs_in_gki-android12-5.10.patch

## 用户偏好（务必遵守）

- 未明确要求不 push；构建参数默认 resukisu+susfs+kpm（用户卖包配置）
- 产物命名：保留 GitHub artifact 名，下载归档由用户指定目录（AGENTS.md 下载规范）
- 交接文档 `docs/当前会话交接.md` 每个关键节点都要更新（本轮的 5x 进展我还没写进去，接手后先补上）
- 闲鱼文案已定稿：全平台合并一条（¥35/位 校验版），在上一段会话里，与本任务无关

name: "T-M call TJ only"
on:
  workflow_dispatch:
    inputs:
      t1:
        description: t
        required: false
        type: string
        default: ''
jobs:
  a:
    uses: ./.github/workflows/_t_j.yml
    with:
        t1: ${{ inputs.t1 }}
    secrets: inherit